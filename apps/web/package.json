{
  "name": "telmed-web",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "cd ../.. && eslint \"apps/web/src/**/*.{ts,tsx}\" --report-unused-disable-directives --fix",
    "typecheck": "tsc -p tsconfig.app.json --noEmit"
  },
  "dependencies": {
    "@livekit/components-react": "^2.4.0",
    "leaflet": "^1.9.4",
    "livekit-client": "^2.5.0",
    "react-leaflet": "^5.0.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "typescript": "^5.5.4"
  }
}
