{
  "name": "telmed-web",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "cd ../.. && eslint \"apps/web/src/**/*.{ts,tsx}\" --report-unused-disable-directives --max-warnings 0 --fix",
    "typecheck": "tsc -p tsconfig.app.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
